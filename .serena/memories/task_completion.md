Before handing work back:
- Run `npm run build` if changes affect TypeScript or bundling (ensures type-check + Vite build succeed).
- Manually verify affected UI logic in the dev server when practical (React forms + preview interactions).
- Summarize code changes referencing file paths/lines and mention any tests or manual verification performed or skipped.
